<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eus.ehu.tta.upv_ehutour">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vista.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".vista.MapActivity"
            android:label="@string/title_activity_map"
            android:theme="@style/AppTheme" />
        <activity android:name=".vista.PolideportivoActivity" />
        <activity android:name=".vista.PlazaLaboaActivity" />
        <activity android:name=".vista.ArboretumActivity" />
        <activity android:name=".vista.BibliotecaActivity" />
        <activity android:name=".vista.HosteleriaActivity" />
        <activity android:name=".vista.BusActivity" />
        <activity android:name=".vista.MagisterioActivity" />
        <activity android:name=".vista.SecretariaActivity" />
        <activity android:name=".vista.CafeteriaActivity" />
        <activity android:name=".vista.ComedorActivity" />
        <activity android:name=".vista.SalaEstudiosActivity" />
        <activity android:name=".vista.AulasActivity" />
        <activity android:name=".vista.DespachosActivity" />
        <activity android:name=".vista.SalaActosActivity" />
        <activity android:name=".vista.SalaOrdenadoresActivity" />
        <activity android:name=".vista.TalleresActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".vista.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".vista.RegistroActivity"></activity>
    </application>

</manifest>