<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eus.ehu.tta.upv_ehutour">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MapActivity"
            android:label="@string/title_activity_map"
            android:theme="@style/AppTheme" />
        <activity android:name=".PolideportivoActivity" />
        <activity android:name=".PlazaLaboaActivity" />
        <activity android:name=".ArboretumActivity" />
        <activity android:name=".BibliotecaActivity" />
        <activity android:name=".HosteleriaActivity" />
        <activity android:name=".BusActivity" />
        <activity android:name=".MagisterioActivity"></activity>
    </application>

</manifest>